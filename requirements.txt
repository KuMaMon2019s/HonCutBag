# HonCut — legacy Conda/runtime compatibility dependencies
# The authoritative development environment is pyproject.toml + uv.lock.
# Install and test with `make install` / `make test`; do not invoke bare pip or pytest.
# Generated: 2026-07-31 (from actual import scan of pipeline/src + vendor/)

# --- Core (hard requirements) ---
openai>=2.50.0            # LLM client (ARK-compatible) — adaptation/discovery/storyboard/director
requests>=2.34.0          # HTTP — seedance_client, seedream_client, tos_uploader, shot_embedder
PyYAML>=6.0               # pipeline_config.py
python-dotenv>=1.0.0       # project-root .env loading for pipeline subprocesses

# --- Orchestration (required; startup fails with an install command if missing) ---
langgraph>=1.2.0
langgraph-checkpoint-sqlite>=3.1.0
langchain-core>=1.5.0
websockets>=12.0
arq>=0.28.0

# --- Visual analysis (required by Phase 8 frame QA and image gates) ---
numpy>=1.24.0
Pillow>=10.0.0
scenedetect>=0.6.4,<0.7     # Core scene boundary detection (headless-compatible)
opencv-python-headless>=4.10

# --- System binaries (NOT pip — install via Homebrew) ---
#   brew install ffmpeg          # ffmpeg + ffprobe (64 + 15 call sites)
#   brew install node            # node (Remotion caption burn)
